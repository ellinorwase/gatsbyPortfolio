import React from "react"
import Meny from "./menu"

export default () => (

        <nav id="slide">
          <ul className="menu" style={{ textDecoration: `none` }} id="navbar">
          <Meny/>
          </ul>
        </nav>
)
